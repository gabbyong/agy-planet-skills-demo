# 3Blue1Brown Animations Reference (Manim)

Use this reference to generate 3Blue1Brown-style mathematical and algorithmic vector animations using Manim Community Edition.

## What Good Looks Like

- **Canvas & Palette**: Use a dark charcoal background (`#0e0e0e`), clean typography, and 3Blue1Brown signature accent colors (`BLUE`, `YELLOW`, `TEAL`, `GREEN`, `RED`).
- **Coordinate Boundaries**: Keep all geometry and text strictly within canvas bounds using `.next_to()` buffers to prevent overlapping.
- **Dynamic Transforms**: Prefer smooth vector morphs (`ReplacementTransform`, `LaggedStart`, `Create`, `MoveAlongPath`) rather than abrupt cuts.
- **3D Loss Landscapes**: Use `ThreeDScene`, `ThreeDAxes`, and `Surface` with `self.begin_ambient_camera_rotation(rate=0.12)` to visualize optimization surfaces. Always lock HUD text using `self.add_fixed_in_frame_mobjects(title, legend)`.
- **Audio-Visual Harmony**: Synchronize scene pacing directly with TTS narration tracks.

## Rendering Pipeline

Run Manim on demand with `uv`:

```bash
uv run --with manim manim -qm scene.py MyScene -o /tmp/animation_raw.mp4
```

Flags:
- `-ql`: 480p 15fps (fastest scratch preview)
- `-qm`: 720p 30fps (standard web embed)
- `-qh`: 1080p 60fps (high fidelity)

## Combining with Voiceover Narration

After generating the narration audio, mux the audio and video using FFmpeg:

```bash
ffmpeg -y -i /tmp/animation_raw.mp4 -i /tmp/narration.mp3 -c:v copy -c:a aac /tmp/final_explainer.mp4
```

## Reference Examples

See the complete 3D surface optimization example in:
- [**`examples/adam-3d-optimization/`**](../examples/adam-3d-optimization): Contains [`adam_3d_animation.py`](../examples/adam-3d-optimization/adam_3d_animation.py) and the rendered narrated MP4 [`adam_3d_narrated.mp4`](../examples/adam-3d-optimization/adam_3d_narrated.mp4).
